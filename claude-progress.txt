=== sangyong_llm 진행 기록 ===

## 2026-08-13

파이썬 코드 생성용 53M 트랜스포머를 밑바닥부터 구현하는 프로젝트를 시작해
프리트레이닝 직전까지 완료했다. PyTorch는 텐서/autograd/CUDA/융합 어텐션
커널만 쓰고, 토크나이저·어텐션·RoPE·RMSNorm·학습 루프·데이터 파이프라인·평가
채점기는 전부 직접 작성했다.

각 단계는 먼저 깨뜨려보고 통과한 것만 다음으로 넘기는 방식으로 진행했다.
누적 56개 검증 항목이 통과 상태다.

### 완료

- 0단계 환경 (9/9): Python 3.14에 CUDA 휠이 없어 3.12 venv를 별도로 만들고
  torch 2.11.0+cu128 설치. GPU 행렬곱이 CPU와 일치(6.1e-05), bf16, GQA SDPA,
  가용 VRAM 4.90GB 확인.
- 1단계 토크나이저 (13/13): 바이트 단위 BPE 직접 구현. 적대적 입력 28종과
  무작위 유니코드 1000건 왕복 무손실.
- 2단계 모델 (14/14): RoPE/RMSNorm/GQA/SwiGLU. 파라미터 53,507,200개로 설계
  손계산과 정확히 일치. 인과 누설 최대차 0.00e+00.
- 3단계 학습 루프 (8/8): AdamW, 코사인 스케줄, bf16, 체크포인트/재개.
  단일 배치 과적합 loss 5.609 -> 0.00000. 재개 궤적 차이 0.00e+00.
- 평가 하네스 (12/12): 정답 10개 통과 / 오답 10개 탈락. sys.exit(0),
  os._exit(0) 우회 차단.
- 데이터: codeparrot-clean 8샤드 필터 완료. 419,773 문서 / 3.63GB
  (유지율 52.5%).

### 잡은 버그

1. 토크나이저 어휘 크기가 조용히 줄어드는 문제. 작은 코퍼스에서 병합이
   소진되면 vocab_size=1000을 요청해도 400이 나왔다. 모델 임베딩과 어긋나는
   순간 터진다. 예약 토큰으로 크기를 불변으로 만들고 경고를 붙였다.
2. BinDataset이 memmap을 안 닫아 Windows에서 파일이 잠기는 문제. 학습 중
   데이터 교체가 막힌다. close()와 컨텍스트 매니저를 추가했다.
3. 채점기가 sys.exit(0)에 속는 문제. 종료코드 0만 보면 테스트를 건너뛴
   코드가 통과로 잡힌다. 센티넬 출력 확인을 추가했다.

### 데이터셋 선정 시행착오

the-stack-v2와 stack-edu 둘 다 본문이 없고 blob_id만 담고 있어
Software Heritage S3를 따로 거쳐야 한다. codeparrot-clean이 content와
license를 직접 담고 있고 gated도 아니어서 이쪽으로 교체했다.

### 데이터 완료

- 토크나이저 실학습: 100MB 표본, 병합 16,127개(예약 0), 압축률 3.763
  바이트/토큰. held-out 3,000문서 28.3MB 왕복 무손실 (7/7 검증 통과).
- 전체 토큰화: train 1,027,146,559 토큰 / val 2,103,160 토큰.
  53M 모델의 Chinchilla 최적치(약 1.07B)와 거의 일치.
- 바이너리 디코딩 육안 확인 완료 — 정상적인 파이썬 소스가 나온다.

### GPU 경합 이슈 (해소됨)

사용자의 PPO 강화학습(sim.train.ppo, 16384 env)이 GPU를 98% 점유하고
있어서 클럭이 3105MHz 중 645MHz로 눌려 있었다. 속도 측정이 전부 오염됐다.
사용자 판단으로 PPO 종료를 기다렸고, 종료 후 클럭이 2160~2385MHz로 복귀했다.

### VRAM 실측 (중요)

Windows WDDM은 VRAM을 넘겨도 OOM을 내지 않고 시스템 RAM으로 조용히
흘려보낸다. 그래서 "동작은 하는데 20배 느린" 상태가 된다. OOM 여부로
배치 크기를 정하면 안 되고, 최대 할당량이 안전선(전체의 85%)을 넘는지와
스텝 시간이 튀는지를 함께 봐야 한다. 결론: batch_size 4 (peak 4.45GB),
기울기 누적 32 -> 유효 배치 131,072 토큰/스텝.

### 진행 중

53M 본 프리트레이닝 시작. 7,800스텝 = 약 1.02B 토큰 = 1에포크.
초기 loss 9.8597 (ln(16384)=9.70과 일치), VRAM 4.26GB, GPU 100% 가동.

### 남은 일

- 본 학습 완료 후 eval/harness.py로 pass@k 측정
- 인스트럭션 튜닝, 검색/툴 레이어 (기반 모델이 나온 뒤)

### 알려진 한계

6GB VRAM이 상한이다. 1B 파라미터는 물리적으로 안 들어가고 들어가도 약
9개월이 걸린다. Claude Opus 5급(학습비 $200M~500M)은 범위 밖이며 이는
구현 문제가 아니라 하드웨어 문제다.

## 2026-08-14 — 검색/툴 레이어

tools/protocol.py, tools/search.py, tools/pipeline.py, tests/test_tools.py를
추가했다(40/40 통과). 토크나이저와 학습 중인 체크포인트는 건드리지 않았다.
툴 호출 포맷은 특수 토큰 없이 한 줄짜리 일반 텍스트 마커 `### 검색: <질의>`로
정했다. 닫을 것이 없어 53M 모델이 배우기 쉽고 기존 `### 지시:` 형태와 같은
모양이다. 파서는 줄 시작 마커만 인정하고 코드블록/삼중따옴표 구간은
마스킹한다. 잘린 호출과 빈 질의는 무시하되 ParseIssue로 보고한다.
검색은 Brave/Tavily/Serper 세 제공자를 SearchResult로 정규화하고, HTTP는
주입 가능한 fetch로 빼서 테스트에서 네트워크를 쓰지 않는다. 키 없음 /
429 / 401 / 타임아웃 / 네트워크 / 깨진 JSON을 각각 다른 예외로 구분한다.
컨텍스트 포맷터에서 실제 문제를 하나 잡았다: 한국어 스니펫이 이 토크나이저
에서 문자당 토큰 비용이 높아, 300자 스니펫 하나가 240토큰 예산을 통째로
먹고 포함 0개/생략 5개가 나왔다. 결과 하나당 예산 상한(기본 budget/3)을
두고 스니펫만 줄이되 제목+url 머리줄은 남기도록 고쳤다.
참고: 한글 마커는 9~15토큰, 같은 뜻의 ASCII 마커는 4~5토큰이다. SFT 시작
전이라면 마커를 ASCII로 바꾸는 것이 컨텍스트 예산상 유리하다(상수 한 줄).

## 2026-08-18 — ASCII 마커 전환 + 전체 코퍼스로 재학습

### 1B 토큰 학습의 결말

iter 250에서 중단했다. 죽은 게 아니라 노트북이 4일간 절전에 들어갔다가
깨어난 것이고, 프로세스는 정상 생존했다(로그의 4 tok/s는 4일 공백이
경과시간으로 계산된 값이다). 마지막 지표는 train 3.7225 / val 3.7627 /
ppl 43.06으로, train과 val이 거의 붙어 있어 과적합 징후는 없었다.
체크포인트는 pretrain_1B_iter200_backup.pt로 보존했다.

### ASCII 마커 전환

실측: "### 검색:" 9토큰 -> "### Search:" 3토큰, "### 검색결과:" 15 -> 4,
"### 지시:" 9 -> 4, "### 코드:" 9 -> 3. 합계 51 -> 19토큰.
검색 한 라운드가 33 -> 12토큰이 된다. 컨텍스트가 1,024뿐이라 라운드마다
21토큰은 결과 스니펫 하나 분량이다.

전환하면서 테스트 설계 결함을 하나 고쳤다. 픽스처가 마커를 상수가 아니라
리터럴로 박아둬서, 상수를 바꾸자 파서 테스트 12개가 한꺼번에 실패했다.
증상이 "파서가 깨졌다"로 보여서 원인 찾는 데 시간이 든다. 픽스처-상수
일치를 먼저 검사하는 가드 테스트를 앞에 세웠고, 잘린 마커 테스트는
CALL_MARKER[:-2]로 파생시켜 다시는 어긋나지 않게 했다(리터럴 "### 검"은
마커가 바뀌자 더 이상 접두사가 아니게 되어 엉뚱하게 실패했다).

### 분리 실행

며칠짜리 학습이 세션·터미널에 묶여 있으면 안 된다.
scripts/train_detached.py를 만들고 CREATE_NEW_PROCESS_GROUP |
DETACHED_PROCESS로 띄운다. 부모를 죽인 뒤에도 자식이 계속 도는 것을
실제로 확인했다(tick 0 -> 6).

### 학습 설정 변경

- max_iters를 하드코딩하지 않고 train.bin 크기와 --epochs로 계산한다.
  하드코딩하면 코퍼스를 늘릴 때 lr 스케줄이 조용히 어긋난다.
- eval_interval 250 -> 1000. 5만 스텝 규모에서 250마다 평가하면 평가에만
  10시간을 쓴다.
- ckpt_interval 100 -> 250 (약 55분치).
- warmup_iters 200 -> 500 (총 스텝의 1% 안팎).

### 전체 코퍼스

codeparrot-clean 54샤드 전량. 8샤드에서 1.03B 토큰이 나왔으므로 약 6.9B
토큰이 예상된다. 53M 모델에는 Chinchilla 최적(1.07B)의 6.5배지만, 과학습은
현대 표준 관행이고 추론 비용이 그대로라 실사용에 유리하다. 사용자 판단으로
모델 크기는 53M을 유지한다.

토크나이저는 재학습하지 않는다. 100MB 표본에서 이미 압축률 3.763이 나왔고
16k vocab BPE는 표본을 키워도 병합이 거의 수렴한다. 재학습하면 CPU 한 시간을
쓰고 얻는 게 거의 없다.

[검증 - correctness] 툴/SFT 레이어 적대적 검증. 실행으로 확인한 반례 5건.
(1) 파이프라인이 정규화 전 원본 출력을 call.end로 잘라 CRLF 출력에서 호출 줄이
단어 중간에서 끊긴 채 주입됨. (2) _clean_query가 안에서 인용한 따옴표까지 벗겨
'"list" vs "tuple"' -> 'list" vs "tuple'로 질의를 조용히 망가뜨림. (3) 검색 결과
스니펫이 줄 머리에 "### 검색:"/"### 답변:"을 만들어 프로토콜을 위조, 다음 턴에
검색 제공자가 고른 질의가 그대로 재검색됨. (4) estimate_loss가 train 커서를
소모해 에포크당 샘플 일부(기본 설정 약 10%)가 기울기를 못 받음. (1)~(4)는 확인
시점에 이미 수정돼 있어 반례로 재검증만 함. (5) 신규: title/snippet과 달리 url은
공백 정규화를 안 해 url 안 개행이 머리줄을 두 줄로 쪼개고 뒷줄이 마커 자리가 됨
(_neutralize_markers가 첫 줄만 봐서 그대로 통과). protocol.py에서 url 공백 제거 +
마커 무력화를 줄 단위로 바꿔 수정, tests/test_tools.py에 위조 4종 회귀 테스트 추가.
SFT 손실 마스킹은 실제 데이터 3,000샘플 전수 검사로 위반 0건 (off-by-one 없음).
전 스위트 재실행: tools 47/47, sft 23/23, model 14/14, tokenizer 13/13, eval 12/12,
training 8/8. 토크나이저/어휘 16,384 불변, GPU 미사용.

[통합 점검] scripts/run_tests.py에 SFT/툴/적대적 회귀 스위트 3개를 추가하고
전 스위트를 실제로 재실행했다: 9+13+14+8+12+23+47+7 = 133/133 통과, 8개 스위트
전부 통과(runner exit 0). 기존 5개 스위트는 새 코드로 인해 깨진 것이 없다.
tokenizer/tokenizer.json은 sha256 da9ed45b...58c4a8, mtime 08-13 18:26으로
어제 학습 이후 그대로다. 로드해서 vocab_size 16,384 / specials
['<|endoftext|>'] / merges 16,127 확인했고, 새 마커(### 지시:/### 코드:/
### 검색:/### 검색결과:/### 답변:)는 전부 기존 어휘로 인코딩된다(최대 id
9,345, 왕복 무손실). 특수 토큰 추가는 어디에도 없다 — finetune/format.py는
allow_special=False로 인코딩한다. train/train.py(08-14 11:22)와 data/는
툴/SFT 작업(12:08 이후)보다 앞서고, checkpoints/에는 학습 프로세스가 쓰는
latest.pt와 로그만 갱신됐다(sft 하위 폴더는 안 생김 — 테스트가 임시 폴더를
쓴다). 점검 중 프리트레이닝은 계속 살아 있었다(iter 200, loss 9.86 -> 4.04).
CPU 테스트를 돌리는 동안 처리량이 10,100 -> 7,300 tok/s로 잠깐 눌렸다가
회복했다. README에 finetune/·tools/ 구성 항목, SFT/검색 실행 절차, 검증 표
3줄을 추가하고 한계 항목을 갱신했다(기반 모델 없어 SFT 미실행, 검색은 API
키 필요, 한국어 마커의 토큰 비용).
