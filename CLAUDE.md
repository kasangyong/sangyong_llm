# sangyong_llm — 프로젝트 규칙

파이썬 코드를 생성하는 트랜스포머를 밑바닥부터 만드는 프로젝트다.

## 이 프로젝트의 전제

PyTorch에서 빌려 쓰는 것은 **텐서 연산 / autograd / CUDA / 융합 어텐션 커널**
네 가지뿐이다. 그 밖의 것은 직접 구현한다.

**절대 도입하지 말 것**: `transformers`, `tokenizers`, `datasets`,
`accelerate`, `peft`. "그냥 HuggingFace 쓰면 되는데요"는 이 프로젝트의
목적을 부정하는 제안이다. 편의 라이브러리를 제안하기 전에 이 문단을 다시 읽을 것.

## 작업 방식

**만들기 전에 깨뜨린다.** 각 단계마다 적대적 검증을 먼저 작성하고, 통과한
것만 다음 단계로 넘긴다. 판정은 위험 / 보통 / 통과 세 가지이고 **통과일
때만** 진행한다.

**증거를 낸다.** "동작합니다"는 보고가 아니다. 실제로 실행한 출력과 수치를
붙인다. 테스트가 통과했다는 사실 자체를 근거로 삼지 말 것 — 테스트가 허술할
수 있으니 그 테스트가 무엇을 검증하는지 읽어야 한다.

테스트는 "동작하는지" 확인이 아니라 **"어떻게 깨지는지" 찾는 것**이 목적이다.
적대적 입력, 경계 조건, 조용히 틀리는 경우를 노린다.

## 어휘는 16,384로 고정이다

`tokenizer/tokenizer.json`을 다시 학습하거나 특수 토큰을 추가하면 임베딩
크기가 바뀌어 **기존 체크포인트가 전부 무용지물이 된다.** 포맷 마커가
필요하면 일반 텍스트를 쓴다(`### Instruction:`, `### Search:` 등).

토크나이저를 바꿔야만 한다면 모든 체크포인트를 버리고 데이터를 다시 토큰화한
다음 처음부터 학습하는 것이다. 그 비용을 먼저 말하고 승인을 받을 것.

## 코드 스타일

- 주석과 출력은 한국어. 식별자는 영어.
- 주석은 "무엇을"이 아니라 **"왜"**를 쓴다. 판단 근거가 있는 곳에만 단다.
- 과장 금지. "CRITICAL", "매우 중요" 같은 표현을 쓰지 않는다.
- 테스트는 `tests/test_model.py` 형식을 따른다: `RESULTS` 리스트 +
  `check(name, fn)` + `[PASS]`/`[FAIL]` + "결과: N/M 통과" + "판정: 통과/위험"
  + `sys.exit(0 또는 1)`.
- 모든 실행 스크립트 상단에
  `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.
  Windows cp949 콘솔에서 한글 출력이 죽는다.

## 아는 함정 (다시 밟지 말 것)

**Windows WDDM은 VRAM 초과에 OOM을 내지 않는다.** 드라이버가 조용히 시스템
RAM으로 흘려보내 "돌지만 20배 느린" 상태가 된다. `try/except OutOfMemoryError`로
배치 크기를 정하면 안 된다. `scripts/probe_vram.py`가 최대 할당량과 스텝
시간을 함께 본다.

**SFT 손실 마스킹은 `labels[:n_prompt-1]`이다.** `n_prompt`가 아니다. 모델
forward는 시프트를 안 하고 dataset이 시프트하므로, 완성 첫 토큰을 예측하는
자리를 살려야 "프롬프트를 다 읽고 코드를 시작하는" 전이를 배운다. 한 칸 더
가리면 손실 곡선은 멀쩡한데 그 전이만 못 배운다.

**인과 마스크 누설은 손실 곡선으로 못 잡는다.** 뚫려 있으면 손실은 예쁘게
떨어지고 생성만 안 된다. `tests/test_model.py`의 누설 테스트를 항상 통과시킬 것.

**채점기는 종료코드만 보면 안 된다.** 생성 코드가 `sys.exit(0)`을 부르면
테스트를 건너뛰고 0을 반환한다. 센티넬 출력을 확인한다.

**KV 캐시가 찬 상태의 다중 질의는 지원하지 않는다.** `F.sdpa`의 `is_causal`이
마스크를 좌상단 기준으로 정렬해 조용히 틀린 답을 낸다. `ValueError`를 던진다.

## 검증 실행

```bash
python scripts/run_tests.py
```

8개 스위트 140항목. GPU가 학습으로 점유 중이면 `verify_env.py`의 가용 VRAM
검사가 걸릴 수 있는데, 그건 올바른 동작이다.

## 며칠짜리 학습

터미널·세션에 묶어서 띄우지 말 것. 접속이 끊기면 같이 죽는다.

```bash
python scripts/train_detached.py start
python scripts/train_detached.py status
```

체크포인트는 모델 가중치뿐 아니라 옵티마이저 모멘텀과 스텝 수까지 저장한다.
`--resume`으로 궤적이 끊기지 않고 이어진다(테스트로 확인: 재개 전후 손실
차이 0.00e+00).
