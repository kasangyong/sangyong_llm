# sangyong_llm — 에이전트 지침

작업 규칙은 **[CLAUDE.md](CLAUDE.md) 한 파일**로 관리한다. 이 파일을 읽고
있다면 `CLAUDE.md`를 열어서 그쪽 내용을 따를 것.

규칙을 여기에 복사해 두지 않는 이유는 드리프트 때문이다. 같은 내용이 두
파일에 있으면 규칙을 고칠 때 한쪽만 고치게 되고, 그 시점부터 어느 쪽이
맞는지 알 수 없어진다.

`CLAUDE.md`에 들어 있는 것:

- 이 프로젝트가 HuggingFace 라이브러리를 쓰지 않는 이유와 그 범위
- "만들기 전에 깨뜨린다" 작업 방식과 통과/보통/위험 판정
- 어휘 16,384 고정 제약
- 코드 스타일 (한국어 주석, 테스트 형식, UTF-8 재설정)
- 이미 밟은 함정들 — Windows WDDM의 VRAM 무성 유출, SFT 손실 마스킹
  off-by-one, V100의 bf16 에뮬레이션, 인과 마스크 누설, 채점기 우회
